/**
 * Chart.js + datalabels entry — di-load on-demand untuk halaman analitik.
 */
import Chart from 'chart.js/auto';
import ChartDataLabels from 'chartjs-plugin-datalabels';

window.Chart = Chart;
window.ChartDataLabels = ChartDataLabels;
